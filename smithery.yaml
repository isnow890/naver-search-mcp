# Smithery.ai configuration for TypeScript runtime
# For remote deployment, only 'runtime' field is required
# Metadata (name, version, description, author, license) is read from package.json
# Config schema should be exported from src/index.ts as 'configSchema'

runtime: typescript
